apiVersion: apps/v1
kind: Deployment
metadata:
  name: ml-ru
spec:
  replicas: 1
  selector:
    matchLabels:
        app: ml-ru-app
  template:
    metadata:
      labels:
        app: ml-ru-app
    spec:
      volumes:
      - configMap:
          defaultMode: 0755
          name: ml-rB-config
        name: ml-config
      containers:
      - name: ml-ru-service
        image: ghcr.io/giros-dit/across-monitoring-stack/ml-dummy:latest
        command: ["python3", "-u", "/ml/ml.py"]
        args: ["TP_ru", "ML_ru"]
        volumeMounts:
        - name: ml-config
          mountPath: /ml/ml-config.txt
          subPath: ml-rB-config.txt
      
        
        